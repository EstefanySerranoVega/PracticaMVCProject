var cardProducto = document.getElementById('card_producto');
cardProducto.addEventListener('submit', function(e) {

    e.preventDefault();
    /*
    // error_log('form es: ' + cardProducto);
    var datos = new FormData(cardProducto);
    let parrafo = document.getElementById('text-hero').value;
    let name = document.getElementById('nombre_producto').value;
    console.log('submit reconocido mediante click cod: ');
    alert('parrafo es: ' + parrafo);
    alert('name es: ' + name);
    console.log(datos.get('nombre_producto'));
    */
});